import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  contain: {
    paddingVertical: 10,
    flexDirection: 'column',
    alignItems: 'center',
  },

  thumb: {
    width: 120,
    height: 120,
    borderRadius: 60,
    borderWidth: 2,
    marginBottom: 10,
  },
});
